import React, { Component } from 'react';

class LoginButton extends Component {
  render() {
    return <button type="button" className="button btn">Sign up</button>
  }
}

export default LoginButton;