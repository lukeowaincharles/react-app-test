import React, { Component } from 'react';

class SigninButton extends Component {
  render() {
    return <button type="button" className="button btn">Sign in</button>
  }
}

export default SigninButton;